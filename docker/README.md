## Docker notes

- `docker-compose.yml` runs `postgres`, `backend`, `frontend`.
- Optional Google service account JSON can be mounted as a secret and referenced via `GOOGLE_SERVICE_ACCOUNT_KEY_PATH`.

